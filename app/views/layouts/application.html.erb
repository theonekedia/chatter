<!DOCTYPE html>
<html>
<head>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
</head>
<body>
    <header>
        <div class="wrapper">
            <%= image_tag("gfx/logo.png") %>
            <span>Chatter</span>
            <% if current_user %>
            <%= link_to "Public Chats", chats_path %> &nbsp
            <%= link_to "Public Profiles", users_path %> &nbsp
            <%= link_to "My Profile", current_user %> &nbsp
            
            <%= link_to "Buddies Chats", buddies_path %> &nbsp
                <%= link_to "Log Out", sessions_destroy_path %>
            <% else %>
            <%= form_tag sessions_create_path do %>
                <%= text_field_tag :username, nil, placeholder: "username" %>
                <%= password_field_tag :password, nil,  placeholder: "password" %>
                <%= submit_tag "Log In" %>
            <% end %>
    </div>
<% end %>
    </header>
    <div id="content">
        <div class="wrapper">
            <% flash.each do |name, msg| %>
                <%= content_tag :div, msg, class: "flash #{name}" %>
            <% end %>
            <%= yield %>
        </div>
    </div>
    <footer>
        <div class="wrapper">
            Chatter - A innovative way to chat.<%= image_tag("gfx/logo-nettuts.png") %>
        </div>
    </footer>
</body>
</html>